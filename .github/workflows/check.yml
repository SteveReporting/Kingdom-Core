name: Kingdom Core Check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Syntax check
        run: npm run check
      - name: Import consolidated setup2 modules
        run: |
          node -e "Promise.all([import('./src/commands/setup2.js'),import('./src/services/setup2Ultimate.js'),import('./src/services/carryTickets.js'),import('./src/services/applicationLinks.js'),import('./src/services/ticketControlV2.js'),import('./src/services/hierarchySetup2.js'),import('./src/ui/premiumUi2.js')]).then(()=>console.log('setup2 module imports OK'))"
      - name: Import setup3 addon modules
        run: |
          node -e "Promise.all([import('./src/commands/setup3.js'),import('./src/services/carryPartiesV3.js'),import('./src/services/carrierDepartmentV3.js'),import('./src/services/serverStatsVerification.js')]).then(()=>console.log('setup3 module imports OK'))"
      - name: Import setup4 platform modules
        run: |
          node -e "Promise.all([import('./src/commands/setup4.js'),import('./src/services/platformV4.js'),import('./src/services/platformV4Runtime.js'),import('./src/services/platformV4Interactions.js'),import('./src/services/platformApiV4.js'),import('./src/services/securityV4.js')]).then(()=>console.log('setup4 platform imports OK'))"
